<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>To Do Express</title>
  <link rel="stylesheet" href="static/styles.css">
</head>

<body>
  <h1>Todo List</h1>
  <div class="form">
    <form class="form" id="add-item" action="/" method="post">
      <input type="text" name="item" placeholder="Add a todo" value="">
      <button type="submit" form="add-item" name="button">Add todo</button>
    </form>
  </div>
  <div>
    <p class="error-msg">{{errorMessage}}&nbsp;</p>
  </div>
  <div class="todo-list">
    <h2>Tasks to complete</h2>
    <form class="list" action="/list" method="post">
      <ul>
        {{#todos}}
        <div class="todo-new">
          {{^completed}}
          <li><button type="submit" id="{{id}}" name="check" value="{{id}}">Mark Complete</button>{{item}}</li>
          {{/completed}}
        </div>
        {{/todos}}
      </ul>
      <h2>Completed tasks</h2>
      <ul>
        {{#todos}}
        <div class="todo-completed">
          {{#completed}}
          <li><span class="completed-item">{{item}}</span></li>
          {{/completed}}
        </div>
        {{/todos}}
      </ul>
    </form>
  </div>
</body>
</html>
